hardware
- sensor to code home position and absolute positions of stage
- stoppers at end to not make stage fall off
- check raspberry 5 for vibration resilience
- add m.2 ssd for fast video writing

new motor driver software
- read_current_speed does not properly return negative values!?
  - possible also the write_ method has the same problem?
  - check github.com/Mickael-Roger/python-st3215 is different code?
- operating_mode calls should be part of driver?
  - in general, why does the servo class not wrap the sram/eeprom calls?
- all SERVOS[ax]['current_speed'] should probably be discarded?
- torque limiting not yet coded
- ST_MAX_ID not used: should patch servo driver code
- make a requirements.txt with pip-chill

software (ideas from last flight)
- with threads, should use thread.join() (why, to wait at end?)
- switch UI mode to pynput, test on raspian
- test UI also over high-latency link (vnc etc)
- how to drive motors, can we avoid wheel mode as much as possible?
  - to test, can the goto be interrupted while the motor is driving?
  - if so, could use a large delta to drive and then stop at any time
  - see below of idea of MotorHandler
- torque detection for motors, code recognition of home position
- switch to sync_read to query all positions
- set frame rate for recording, check different modes
- allow to set gain, etc via software
- build a small UI?
- AI recognition of sharp images?


class MotorHandler:
- method to goto specified position
  - call API for goto
  - enter waiting loop to query ismoving, but also some interrupt flag
  - is this loop where we also query for the torque and abort on limit?
  - the method should recall what it set as aim and query if that target changed


NEW

jd wants to rebuild a v2 hardware with stepper motors controlled by tmc2209
for something that comes close, see here:
https://learn.adafruit.com/motorized-camera-slider-2-axis/code-the-slider
doenload the project bundle and pull the adafruit_tmc2209.py from there
